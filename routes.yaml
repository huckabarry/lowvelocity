routes:
  /.well-known/site.standard.publication/:
    template: standard-site-publication
    content_type: text/plain

  /urbanism/:
    controller: channel
    template: custom-urbanism
    filter: 'tag:[urbanism,housing,transportation,public-finance]'
    data: tag.urbanism

  /field-notes/:
    controller: channel
    template: custom-field-notes
    filter: 'tag:field-notes'
    data: tag.field-notes

  /gallery/:
    controller: channel
    template: custom-gallery
    filter: 'tag:[gallery,photography]'
    data: tag.gallery

collections:
  /notes/:
    permalink: /notes/{slug}/
    template: custom-notes
    filter: 'tag:[status,note,notes,afterword]'
    data: tag.status

  /:
    permalink: /{slug}/
    template: index
    filter: 'tag:-[status,note,notes,afterword]'

taxonomies:
  tag: /tag/{slug}/
  author: /author/{slug}/
