{{!< default}}

{{#contentFor "body_class"}} lowvelocity-section-template lowvelocity-notes-template{{/contentFor}}

<main class="site-main lowvelocity-section">

    <section class="lowvelocity-section-head gh-canvas">
        <p class="lowvelocity-section-kicker">Notes</p>
        <h1 class="lowvelocity-section-title">Notes</h1>
        <p class="lowvelocity-section-description">Short status-style posts, quick observations, and smaller updates.</p>
    </section>

    <div class="post-feed gh-feed gh-canvas lowvelocity-notes-feed">
        {{#foreach posts}}
            <article class="feed lowvelocity-note-feed {{visibility}} {{post_class}}">
                <div class="feed-calendar">
                    <div class="feed-calendar-day">{{date published_at format="DD"}}</div>
                    <div class="feed-calendar-month">{{date published_at format="MMM"}}</div>
                </div>

                <div class="lowvelocity-note-feed-body">
                    <h2 class="feed-title lowvelocity-note-title">{{title}}</h2>
                    {{#if custom_excerpt}}
                        <p class="lowvelocity-note-excerpt">{{custom_excerpt}}</p>
                    {{else}}
                        <p class="lowvelocity-note-excerpt">{{excerpt words="34"}}</p>
                    {{/if}}
                </div>

                <div class="feed-right">
                    {{> "icons/star"}}
                    <div class="feed-length">{{reading_time minute=(t "1 min read") minutes=(t "% min read")}}</div>
                </div>

                {{> "icons/chevron-right"}}
                <a class="u-permalink" href="{{url}}" aria-label="{{title}}"></a>
            </article>
        {{else}}
            <p class="lowvelocity-empty">No notes are published yet.</p>
        {{/foreach}}
    </div>

    {{pagination}}

</main>
